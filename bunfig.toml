[install]
# All required peers are explicit dependencies. Avoid installing optional
# framework peers (including Better Auth's optional Vitest peer).
peer = false

# Keep a bare root `bun test` focused on the backend. Cockpit component tests
# run through `bun run cockpit:test` with app/bunfig.toml's DOM preloads.
[test]
root = "tests"

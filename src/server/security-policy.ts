/** Fixed hosted-session policy; values are explicit so library defaults never become product policy. */
export const AUTH_SESSION_EXPIRES_IN_SECONDS = 12 * 60 * 60;
export const AUTH_SESSION_FRESH_AGE_SECONDS = 10 * 60;
